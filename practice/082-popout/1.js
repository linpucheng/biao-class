; (function () {
  'use strict';

  boot();

  function boot() {
    document.addEventListener('click', (e) => {
      let el = e.target;
      let content = el.dataset.popout;

      if (content) {
        
      }
    });
  }
})();